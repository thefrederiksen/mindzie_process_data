# Core dependencies for Hire to Retire process mining dataset generation
# Python 3.7+ required

# HTTP requests for API integration
requests>=2.26.0,<3.0.0

# Environment variable management
python-dotenv>=0.19.0,<1.0.0

# Optional: Data analysis (uncomment if needed for advanced statistics)
# pandas>=1.3.0,<2.0.0
# numpy>=1.21.0,<2.0.0

# Optional: Date handling utilities (using standard library datetime for now)
# python-dateutil>=2.8.0,<3.0.0

# Note: The core event log generation uses only Python standard library
# Additional dependencies are primarily for API uploads and environment configuration